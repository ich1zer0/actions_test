name: Conditions workflow
on: workflow_dispatch
jobs:
  skip-or-run:
    env:
      ACTOR: ${{ github.actor }}
    if: ${{ ACTOR == 'ich1zer0' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "${ACTOR}"
